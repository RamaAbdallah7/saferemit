# Demo video script — moved

Superseded by **[`docs/demo-video-script.md`](../docs/demo-video-script.md)**,
which matches the current site (the interactive walkthrough at `/`, the
working prototype at `/app`, and the parallel-CAMARA-call proof in the
`/api/decide` `timing` block). Use that one.
