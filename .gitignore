__pycache__/
*.pyc
.venv/
venv/
.env
*.log
.DS_Store
node_modules/
dist-old/
.vite/

# frontend/dist is committed on purpose: Render's Python runtime has no
# Node, so the built React bundle ships in the repo and FastAPI serves it.
backend/**/dist/
